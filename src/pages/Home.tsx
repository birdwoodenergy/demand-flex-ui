import React from 'react';
import { Typography } from '@mui/material';

const Home: React.FC = () => {
  return (
    <div>
      <Typography variant="h4"></Typography>
    </div>
  );
};

export default Home;